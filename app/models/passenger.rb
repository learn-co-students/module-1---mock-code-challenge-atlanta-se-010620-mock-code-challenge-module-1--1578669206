class Passenger

    def name(name)
        @name = name
    end

    def rides(rides)
        @rides = rides
    end

    def drivers(drivers)
        @drivers = drivers
    end

    def total_distance(total_distance)
        @total_distance = total_distance 
    end

end
